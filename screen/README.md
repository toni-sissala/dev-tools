when running emacs inside screen, it might be necessary to add the following lines to .bashrc in order to make use of all 256 colors.

# -----------
export TERM=xterm-256color
export TERMCAP=
# ------------
